'use strict';

/**
 * elevate-locale controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::elevate-locale.elevate-locale');
